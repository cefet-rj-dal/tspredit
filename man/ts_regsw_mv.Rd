% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_regsw_mv.R
\name{ts_regsw_mv}
\alias{ts_regsw_mv}
\title{Multivariate Sliding-Window Regressor}
\usage{
ts_regsw_mv(model_y, models_x, window_size = 30)
}
\arguments{
\item{model_y}{A \code{ts_mv_spec} or a fitted-model constructor for the target
variable.}

\item{models_x}{Named list with one \code{ts_mv_spec} (or plain model object) per
auxiliary variable.}

\item{window_size}{Integer. Base window size available to each variable.}
}
\value{
A \code{ts_regsw_mv} object.
}
\description{
Orchestrate one target model and one auxiliary model per
covariate, while reusing the existing univariate learners from \code{tspredit}.
}
\details{
\code{ts_regsw_mv()} is the first multivariate forecasting orchestrator in
\code{tspredit}. It keeps the package centered on a target variable \code{y}, while
allowing every auxiliary variable \verb{x1, ..., xn} to be forecast by its own
pipeline.

The workflow is:
\enumerate{
\item store aligned multivariate observations in \code{ts_data_mv()}
\item define one \code{ts_mv_spec()} for \code{y}
\item define one \code{ts_mv_spec()} for each \code{x}
\item fit the composed system with \code{fit()}
\item forecast recursively with \code{predict(..., steps_ahead = h)}
}

The current implementation keeps a single \code{window_size} as the base temporal
memory available to every variable. After that, each specification decides
which variables and which lag positions are actually used by its learner.

This means the multivariate extension does not replace the existing
univariate models. It reuses them as polymorphic building blocks.

Supported configurations in this first version:
\itemize{
\item the target model must inherit from \code{ts_regsw}
\item auxiliary models may inherit from \code{ts_regsw} or from \code{ts_reg}
\item raw-series auxiliary models such as \code{ts_arima()} currently use only their
own variable as input
}

The method returns the forecast of \code{y} as a numeric vector. The recursive
path of \code{y} and all auxiliary predictions is attached to that vector as
attributes, so the interface stays target-centered without discarding the
system forecast.
}
\examples{
data(tsd)
x1 <- c(tsd$y[-1], tail(tsd$y, 1))
x2 <- stats::filter(tsd$y, rep(1/3, 3), sides = 1)
x2[is.na(x2)] <- tsd$y[is.na(x2)]

mv <- ts_data_mv(data.frame(y = tsd$y, x1 = x1, x2 = x2), y = "y")
samp <- ts_sample(mv, test_size = 5)

model <- ts_regsw_mv(
  model_y = ts_mv_spec(
    ts_mlp(ts_norm_an(), input_size = 4, size = 4, decay = 0),
    variables = c("y", "x1", "x2"),
    transforms = list(y = ts_fil_ma(3))
  ),
  models_x = list(
    x1 = ts_mv_spec(ts_deterministic("periodic", period = 7)),
    x2 = ts_mv_spec(ts_deterministic("periodic", period = 7))
  ),
  window_size = 10
)

model <- fit(model, samp$train)
predict(model, steps_ahead = 1)
predict(model, steps_ahead = 5)
pred <- predict(model, steps_ahead = 5)
attr(pred, "system")
}
