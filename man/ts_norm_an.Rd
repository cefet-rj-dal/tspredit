% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_norm_an.R
\name{ts_norm_an}
\alias{ts_norm_an}
\title{Adaptive Normalization}
\usage{
ts_norm_an(
  outliers = outliers_boxplot(),
  nw = 0,
  average = c("mean", "ema"),
  operation = c("divide", "subtract", "softdivide", "asinh"),
  scale = c("sd", "mad", "none"),
  lambda = 1,
  epsilon = 1e-08
)
}
\arguments{
\item{outliers}{Indicate outliers transformation class. NULL can avoid outliers removal.}

\item{nw}{integer: window size.}

\item{average}{Character. Adaptive reference statistic: \code{"mean"} or \code{"ema"}.}

\item{operation}{Character. Adaptive normalization operator:
\code{"divide"}, \code{"subtract"}, \code{"softdivide"}, or \code{"asinh"}.}

\item{scale}{Character. Local scale estimator used by the hybrid operators:
\code{"sd"}, \code{"mad"}, or \code{"none"}.}

\item{lambda}{Numeric. Weight assigned to the adaptive level term inside the
hybrid reference scale.}

\item{epsilon}{Numeric. Positive floor used to stabilize near-zero
denominators and local scales.}
}
\value{
A \code{ts_norm_an} object.
}
\description{
Transform data to a common scale while adapting to changes in
distribution over time (optionally over a trailing window).
}
\details{
\code{ts_norm_an()} supports a family of adaptive window-wise transformations:
\itemize{
\item \code{"divide"} rescales a window by its adaptive reference level.
\item \code{"subtract"} recenters the window by subtracting the adaptive reference
level.
\item \code{"softdivide"} computes a stabilized relative deviation:
\eqn{(x - \mu) / \sqrt{s^2 + (\lambda \mu)^2 + \epsilon^2}}.
\item \code{"asinh"} applies an inverse-hyperbolic-sine contrast around the adaptive
reference level using the same stabilized scale.
}

The concrete operators are implemented in \code{tsanutils()}, while
\code{ts_norm_an()} focuses on estimating the adaptive references and applying
the chosen transformation consistently during fit, transform, and inverse
transform.

In the current contract, the adaptive reference is estimated from the full
supervised window passed to \code{fit()} or \code{transform()}. So when the input is a
sliding window produced by \code{ts_data()}, the terminal \code{t0} position is part of
the same window-wise reference used for the transformation.

The adaptive reference \eqn{\mu} is estimated either by a simple mean or by an
exponentially weighted mean (\code{average = "ema"}). The hybrid operators
additionally use a local scale estimate \code{s} based on either the standard
deviation or the MAD.
}
\examples{
# time series to normalize
library(daltoolbox)
library(tspredit)
data(tsd)

# convert to sliding windows
ts <- ts_data(tsd$y, 10)
ts_head(ts, 3)
summary(ts[,10])

# divisive adaptive normalization (default)
preproc <- ts_norm_an()
preproc <- daltoolbox::fit(preproc, ts)
tst <- transform(preproc, ts)
ts_head(tst, 3)

# subtractive adaptive normalization
preproc <- ts_norm_an(operation = "subtract")
preproc <- daltoolbox::fit(preproc, ts)
tst <- transform(preproc, ts)
ts_head(tst, 3)

# EMA-based soft division
preproc <- ts_norm_an(average = "ema", operation = "softdivide", scale = "mad")
preproc <- daltoolbox::fit(preproc, ts)
tst <- transform(preproc, ts)
ts_head(tst, 3)
}
\references{
Ogasawara, E., Martinez, L. C., De Oliveira, D., Zimbrão, G., Pappa, G. L.,
Mattoso, M. (2010). Adaptive Normalization: A novel data normalization
approach for non-stationary time series. Proceedings of the International
Joint Conference on Neural Networks (IJCNN). doi:10.1109/IJCNN.2010.5596746

Huber PJ (1964). Robust Estimation of a Location Parameter. Annals of
Mathematical Statistics, 35(1), 73-101. doi:10.1214/aoms/1177703732

Burbidge JB, Magee L, Robb AL (1988). Alternative Transformations to Handle
Extreme Values of the Dependent Variable. Journal of the American
Statistical Association, 83(401), 123-127.

Bellemare MF, Wichman CJ (2020). Elasticities and the Inverse Hyperbolic
Sine Transformation. Oxford Bulletin of Economics and Statistics, 82(1),
50-61. doi:10.1111/obes.12325
}
