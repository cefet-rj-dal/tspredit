% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_window_mv.R
\name{ts_window_mv}
\alias{ts_window_mv}
\title{Multivariate Lagged Windows}
\usage{
ts_window_mv(
  data,
  window_size = 30,
  variables = NULL,
  lags = NULL,
  transforms = NULL
)
}
\arguments{
\item{data}{A \code{ts_data_mv} object or compatible data.frame with aligned
multivariate observations.}

\item{window_size}{Integer. Base number of recent observations available per
variable.}

\item{variables}{Optional character vector. Variables to include in the
materialized window. By default, all variables stored in \code{data} are used.}

\item{lags}{Optional named list with one integer vector per variable. When
omitted, every variable uses all lags from \code{0:(window_size-1)}.}

\item{transforms}{Optional named list of raw-series transformations applied per
variable before the windows are built. Each entry can be a single transform
object or a list of transforms.}
}
\value{
A \code{ts_window_mv} object stored as a data.frame.
}
\description{
Materialize an aligned multivariate series into an explicit
lagged-window table.
}
\details{
\code{ts_window_mv()} is the public bridge between the raw aligned representation
created by \code{ts_data_mv()} and the model-ready structures consumed by the
multivariate forecasting workflow.

The function keeps one base window size for every selected variable, then
expands the aligned data into blocks such as:
\itemize{
\item \verb{y_t6, ..., y_t0}
\item \verb{x1_t6, ..., x1_t0}
\item \verb{x2_t6, ..., x2_t0}
}

When \code{lags} is provided, the function keeps only the requested lag positions
for each variable, which makes it useful for inspecting the exact feature
space seen by a variable-specific pipeline.

Optional \code{transforms} are applied per variable before the lagged blocks are
built. This mirrors the multivariate forecasting workflow, where each
variable may carry its own preprocessing logic.
}
\examples{
data(tsd)
x1 <- c(tsd$y[-1], tail(tsd$y, 1))
x2 <- stats::filter(tsd$y, rep(1/3, 3), sides = 1)
x2[is.na(x2)] <- tsd$y[is.na(x2)]

mv <- ts_data_mv(
 data.frame(y = tsd$y, x1 = x1, x2 = as.numeric(x2)),
 y = "y"
)

windows <- ts_window_mv(mv, window_size = 5)
ts_head(windows, 3)
}
