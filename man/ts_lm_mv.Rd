% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_lm_mv.R
\name{ts_lm_mv}
\alias{ts_lm_mv}
\title{Multivariate Linear Regression}
\usage{
ts_lm_mv(models_x = NULL, formula = NULL, features = NULL)
}
\arguments{
\item{models_x}{Optional named list with one univariate model per auxiliary
variable.}

\item{formula}{Optional regression formula. When omitted, the target variable
from \code{ts_data_mv} is regressed on all auxiliary variables.}

\item{features}{Optional character vector of feature names used when \code{formula}
is \code{NULL}.}
}
\value{
A \code{ts_lm_mv} object inheriting from \code{ts_reg_mv}.
}
\description{
Create a target-centered singular multivariate regressor based on
\code{stats::lm}.
}
\details{
\code{ts_lm_mv()} is a linear-regression member of the \code{ts_reg_mv} family.

It is inspired by the formula-based design already used in \code{daltoolbox},
but adapted to the aligned multivariate time-series abstraction of
\code{tspredit}.

This makes it a very transparent baseline for the singular multivariate
branch:
\itemize{
\item the target variable remains explicit
\item the auxiliary variables are declared in the formula
\item the analyst can read the structural assumption directly from the model
}

The most common usage patterns are:
\itemize{
\item provide a full formula such as \code{y ~ x1 + x2}
\item omit the formula and let the model regress \code{y} on all auxiliary variables
}

The target variable is forecast from the synchronized auxiliary variables.
When future auxiliary values are not known, they can be generated by the
univariate models supplied in \code{models_x}.
}
\examples{
data(tsd)
x1 <- c(tsd$y[-1], tail(tsd$y, 1))
x2 <- stats::filter(tsd$y, rep(1/3, 3), sides = 1)
x2[is.na(x2)] <- tsd$y[is.na(x2)]

mv <- ts_data_mv(data.frame(y = tsd$y, x1 = x1, x2 = as.numeric(x2)), y = "y")
samp <- ts_sample(mv, test_size = 5)

model <- ts_lm_mv(
 models_x = list(x1 = ts_arima(), x2 = ts_arima()),
 formula = y ~ x1 + x2
)
model <- fit(model, samp$train)
predict(model, steps_ahead = 5)
}
\references{
\itemize{
\item Montgomery DC, Peck EA, Vining GG (2021). Introduction to Linear
Regression Analysis. Wiley.
}
}
