% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tslagutils.R
\name{tslagutils}
\alias{tslagutils}
\title{Time Series Lag Utilities}
\usage{
tslagutils()
}
\value{
A \code{tslagutils} object exposing the helper functions.
}
\description{
Utility object that groups helper functions used to select lag subsets for
sliding-window predictors.
}
\details{
These helpers are organized by the type of evidence they use to choose lags.

\strong{Positional mappings}
\itemize{
\item \code{lag_recent()} keeps the most recent lags and reproduces the package's
original behavior.
\item \code{lag_even()} spreads the selected lags evenly across the available window.
\item \code{lag_geom()} emphasizes recent lags while still sampling older history on
a geometric scale.
}

\strong{Correlation-driven mappings}
\itemize{
\item \code{lag_acf()} ranks lags by the absolute autocorrelation of the reconstructed
training series.
\item \code{lag_pacf()} ranks lags by the absolute partial autocorrelation.
\item \code{lag_peaks()} keeps local maxima of the ACF or PACF profile to avoid
selecting many redundant neighboring lags.
\item \code{lag_seasonal()} prioritizes multiples of an estimated or user-provided
seasonal period.
\item \code{lag_acf_seasonal()} and \code{lag_pacf_seasonal()} combine seasonal lags with
correlation-based completion.
\item \code{lag_blocks()} expands neighborhoods around the strongest correlation peaks.
}

\strong{Supervised mappings}
\itemize{
\item \code{lag_mi()} ranks lags by discretized mutual information with the target.
\item \code{lag_mrmr()} greedily maximizes relevance to the target while reducing
redundancy among already selected lags.
}

The mutual-information criteria use quantile discretization and therefore
provide deterministic approximations suitable for lightweight dependency-free
lag selection inside the package.
}
\examples{
utils <- tslagutils()

# Positional baselines
utils$lag_recent(total = 9, input_size = 4)
utils$lag_even(total = 9, input_size = 4)

# Reconstruct a raw series from sliding windows and aligned outputs
data(tsd)
ts <- ts_data(tsd$y, 10)
io <- ts_projection(ts)
series <- utils$reconstruct_series(io$input, io$output)
head(series)

# Correlation profile over available lags
utils$score_acf(series, lag_max = 9)

}
\references{
\itemize{
\item Box GEP, Jenkins GM, Reinsel GC, Ljung GM (2015). Time Series Analysis:
Forecasting and Control. Fifth Edition. Wiley.
\item Hyndman RJ, Athanasopoulos G (2021). Forecasting: Principles and Practice.
Third Edition. OTexts. https://otexts.com/fpp3/
\item Peng H, Long F, Ding C (2005). Feature selection based on mutual
information criteria of max-dependency, max-relevance, and min-redundancy.
IEEE Transactions on Pattern Analysis and Machine Intelligence, 27(8),
1226-1238. doi:10.1109/TPAMI.2005.159
\item Leites J, Cerqueira V, Soares C (2024). Selecting time lags for time
series forecasting: an empirical study. arXiv:2405.11237.
}
}
