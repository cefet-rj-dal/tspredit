% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_regsw_mv.R
\name{ts_mv_spec}
\alias{ts_mv_spec}
\title{Multivariate Model Specification}
\usage{
ts_mv_spec(model, variables = NULL, lags = NULL, transforms = NULL)
}
\arguments{
\item{model}{Base model object. It can be a sliding-window regressor such as
\code{ts_mlp()} or a raw-series model such as \code{ts_arima()}.}

\item{variables}{Optional character vector. Variables used as predictors for
this submodel. When omitted, defaults depend on the context:
target model uses all variables, auxiliary models use their own variable.}

\item{lags}{Optional named list with one integer vector per variable. When
omitted, each variable uses all lags from \code{0:(window_size-1)}.}

\item{transforms}{Optional named list of raw-series transformations applied
per variable before the multivariate windows are built. Each entry can be a
single transform object or a list of transforms. These transformations act as
variable-specific feature engineering and are orchestrated by the
multivariate wrapper.}
}
\value{
A \code{ts_mv_spec} object.
}
\description{
Wrap a univariate forecasting model so it can be orchestrated
inside a multivariate workflow.
}
\details{
\code{ts_mv_spec()} is the object-oriented contract that describes how one
variable-specific predictive pipeline should be assembled inside
\code{ts_regsw_mv()}.

Each specification can declare:
\itemize{
\item \code{model}: the learner responsible for the variable
\item \code{variables}: which synchronized series are allowed as inputs to this
learner
\item \code{lags}: which lag positions are extracted from each variable block
\item \code{transforms}: optional raw-series transformations applied per variable
before the multivariate windows are built
}

This design lets different variables use different forecasting strategies
while preserving a single orchestration contract. For example:
\itemize{
\item the target \code{y} may use \code{ts_lstm(ts_norm_an(), ...)}
\item \code{x1} may use \code{ts_mlp(ts_norm_diff(), ...)}
\item \code{x2} may use \code{ts_rf(ts_norm_gminmax(), ...)} plus a smoothing filter
\item deterministic auxiliary variables may use \code{ts_deterministic()},
\code{ts_periodic()}, or \code{ts_persist()}
}

In other words, the multivariate layer coordinates the pipelines, but the
behavior of each variable still lives inside its own object.
}
\examples{
spec_y <- ts_mv_spec(ts_mlp(ts_norm_gminmax()), variables = c("y", "x1"))
spec_x1 <- ts_mv_spec(ts_deterministic("periodic", period = 7), variables = "x1")
}
