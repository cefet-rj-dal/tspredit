% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_reg_mv.R
\name{ts_reg_mv}
\alias{ts_reg_mv}
\title{Target-Centered Multivariate Regression Base}
\usage{
ts_reg_mv(models_x = NULL)
}
\arguments{
\item{models_x}{Optional named list with one univariate model per auxiliary
variable. These models are used to generate future paths for \verb{x1, ..., xn}
when the target-centered model needs auxiliary forecasts along the horizon.
They are not required when future auxiliary values are supplied directly at
prediction time.}
}
\value{
A \code{ts_reg_mv} object.
}
\description{
Base class for singular multivariate time-series models that
operate on aligned observations (\code{sw = 1}).
}
\details{
\code{ts_reg_mv()} is the multivariate counterpart of the raw-series branch of
\code{tspredit}.

It is intended for models that consume aligned multivariate observations
directly, without first materializing explicit lagged windows in
\code{ts_data_mv(..., sw > 1)}.

This branch is appropriate when the multivariate relationship is naturally
expressed at the aligned-observation level, for example:
\itemize{
\item target-centered linear regression over synchronized covariates
\item ARIMA with external regressors (\code{ARIMAX})
\item vector autoregression over the whole system
}

The design remains target-centered:
\itemize{
\item the multivariate object still declares one target variable \code{y}
\item \code{predict()} returns the forecast of \code{y} by default
\item descendants may also expose the forecast path of the remaining variables
when \code{return_all = TRUE}
}

Typical descendants are:
\itemize{
\item \code{ts_arimax()}: target-centered dynamic regression with ARIMA errors
\item \code{ts_lm_mv()}: target-centered multivariate linear regression
\item \code{ts_var()}: vector autoregression, still exposed through a target-centered
interface
}

The interface keeps a distinguished target variable \code{y}, but models may also
return the forecast path of the remaining variables when requested.
}
