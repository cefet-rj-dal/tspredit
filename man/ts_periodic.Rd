% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_deterministic.R
\name{ts_periodic}
\alias{ts_periodic}
\title{Periodic Deterministic Predictor}
\usage{
ts_periodic(period, context_size = NULL)
}
\arguments{
\item{period}{Integer. Cycle length to repeat.}

\item{context_size}{Optional integer. Most recent values used to identify the
next state within the cycle. When omitted, the smallest non-ambiguous value
is inferred automatically.}
}
\value{
A \code{ts_periodic} object, inheriting from \code{ts_deterministic}.
}
\description{
Forecast a univariate series by repeating a learned periodic
cycle.
}
\examples{
series <- c(4, 5, 6, 7, 1, 2, 3)
model <- ts_periodic(7)
model <- daltoolbox::fit(model, x = series)
predict(model, steps_ahead = 5)
}
