% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_plot_mv.R
\name{plot_ts_pred_mv}
\alias{plot_ts_pred_mv}
\title{Plot Multivariate Forecast Paths}
\usage{
plot_ts_pred_mv(
  history,
  future = NULL,
  prediction,
  variable = NULL,
  label_x = "",
  label_y = "Value",
  color = "black",
  color_adjust = "blue",
  color_prediction = "green"
)
}
\arguments{
\item{history}{A \code{ts_data_mv} or data.frame with the observed history used as
context for the plot.}

\item{future}{Optional \code{ts_data_mv} or data.frame with the held-out aligned
observations. When supplied, the observed future is shown together with the
recursive predictions.}

\item{prediction}{A \code{ts_mv_prediction} object returned by
\code{predict(ts_regsw_mv, return_all = TRUE)}.}

\item{variable}{Optional character scalar. Name of a single variable to plot.
When omitted, plots are returned for every variable in the prediction.}

\item{label_x}{x-axis label.}

\item{label_y}{y-axis label prefix. The variable name is appended when several
plots are returned.}

\item{color}{observed series color.}

\item{color_adjust}{history color.}

\item{color_prediction}{prediction color.}
}
\value{
A single \code{ggplot} object or a named list of \code{ggplot} objects.
}
\description{
Plot observed and forecast trajectories for the target series and
auxiliary variables returned by the multivariate workflow.
}
\details{
\code{plot_ts_pred_mv()} extends the visual logic already used in the univariate
examples. It reuses \code{daltoolbox::plot_ts_pred()} variable by variable and
returns either:
\itemize{
\item one plot when \code{variable} is provided
\item a named list of plots when \code{variable = NULL}
}

The intended workflow is:
\itemize{
\item fit a \code{ts_regsw_mv} model
\item call \code{predict(..., return_all = TRUE)}
\item compare the predicted paths against the held-out aligned multivariate data
}
}
\examples{
library(daltoolbox)
data(tsd)
x1 <- c(tsd$y[-1], tail(tsd$y, 1))
x2 <- stats::filter(tsd$y, rep(1/3, 3), sides = 1)
x2[is.na(x2)] <- tsd$y[is.na(x2)]

mv <- ts_data_mv(data.frame(y = tsd$y, x1 = x1, x2 = as.numeric(x2)), y = "y")
samp <- ts_sample(mv, test_size = 5)

model <- ts_regsw_mv(
  model_y = ts_mv_spec(ts_mlp(ts_norm_gminmax(), input_size = 4), variables = c("y", "x1", "x2")),
  models_x = list(
    x1 = ts_mv_spec(ts_arima()),
    x2 = ts_mv_spec(ts_rf(ts_norm_gminmax(), input_size = 4, ntree = 10), variables = c("x2", "y"))
  ),
  window_size = 5
)
model <- fit(model, samp$train)
pred <- predict(model, steps_ahead = 5, return_all = TRUE)
plots <- plot_ts_pred_mv(samp$train, samp$test, pred)
}
