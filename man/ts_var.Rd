% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_var.R
\name{ts_var}
\alias{ts_var}
\title{Vector Autoregression}
\usage{
ts_var(target = NULL, p = NULL, p_max = 5, intercept = TRUE)
}
\arguments{
\item{target}{Optional target variable name. When omitted, use the \code{y}
attribute stored in \code{ts_data_mv}.}

\item{p}{Optional lag order. When \code{NULL}, choose the order automatically.}

\item{p_max}{Maximum lag order considered in the automatic search.}

\item{intercept}{Logical. Whether to include an intercept in each equation.}
}
\value{
A \code{ts_var} object inheriting from \code{ts_reg_mv}.
}
\description{
Create a target-centered vector autoregression over aligned
multivariate observations.
}
\details{
\code{ts_var()} models the multivariate system directly, but keeps the \code{tspredit}
interface centered on a distinguished target variable \code{y}.

This means:
\itemize{
\item the full system is learned jointly
\item \code{predict()} returns the target forecast by default
\item \code{predict(..., return_all = TRUE)} exposes the forecast path of all system
variables
}

The current implementation uses ordinary least squares over lagged aligned
observations and can choose the lag order automatically by minimizing AICc
over \code{1:p_max}.

This makes \code{ts_var()} conceptually different from \code{ts_arimax()}:
\itemize{
\item \code{ts_arimax()} treats the auxiliaries as regressors for one main target
\item \code{ts_var()} treats all variables as part of the dynamic system
}

Even so, \code{tspredit} still lets the user mark one variable as the main target
for evaluation and default return behavior.
}
\examples{
data(tsd)
x1 <- c(tsd$y[-1], tail(tsd$y, 1))
x2 <- stats::filter(tsd$y, rep(1/3, 3), sides = 1)
x2[is.na(x2)] <- tsd$y[is.na(x2)]

mv <- ts_data_mv(data.frame(y = tsd$y, x1 = x1, x2 = as.numeric(x2)), y = "y")
samp <- ts_sample(mv, test_size = 5)

model <- ts_var(p_max = 3)
model <- daltoolbox::fit(model, samp$train)
predict(model, steps_ahead = 5)
}
\references{
\itemize{
\item Lütkepohl H (2005). New Introduction to Multiple Time Series Analysis.
Springer.
\item Tsay RS (2014). Multivariate Time Series Analysis with R and Financial
Applications. Wiley.
}
}
