% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_warma.R
\name{ts_warma}
\alias{ts_warma}
\title{WARMA}
\usage{
ts_warma(
  preprocess = ts_norm_none(),
  input_size = NA,
  input_map = ts_lagmap(),
  steps = NA,
  intercept = TRUE
)
}
\arguments{
\item{preprocess}{External preprocessing object applied before the WARMA local
steps. Defaults to \code{ts_norm_none()}.}

\item{input_size}{Integer. Number of lagged inputs used by the model.}

\item{input_map}{Lag-selection strategy object created by \code{ts_lagmap()}.}

\item{steps}{Integer in \verb{\{0, 1, 2\}} or \code{NA}. When \code{NA}, infer the smallest
suitable step automatically.}

\item{intercept}{Logical. Whether to include an intercept in the linear model
fitted over the locally normalized lagged inputs.}
}
\value{
A \code{ts_warma} object inheriting from \code{ts_regsw}.
}
\description{
Create a window-based ARMA-inspired regressor with local
stepwise normalization for the \code{ts_regsw} workflow.
}
\details{
\code{ts_warma()} is a \code{tspredit} implementation inspired by the WARMA proposal:
a window-based view of non-stationary series in which local preprocessing is
interpreted in steps.

In this adaptation:
\itemize{
\item step \code{0} leaves the local window unchanged
\item step \code{1} subtracts the local mean of each window
\item step \code{2} subtracts the local mean and scales by the local standard
deviation
}

The implementation follows the package's sliding-window lineage, so it uses
the fully overlapping window regime naturally induced by \code{ts_data(..., sw)}
and \code{ts_regsw}. The resulting representation is then modeled with a linear
regressor over the normalized lagged inputs.

This makes \code{ts_warma()} a computationally light competitor to \code{ts_darima()}
and a practical univariate block for the multivariate target-centered
workflow.

When \code{steps = NA}, the model chooses the smallest step in \verb{\{0, 1, 2\}} whose
locally transformed reconstructed series reaches integration order zero
according to \code{forecast::ndiffs()}.

The current implementation should be understood as the \code{tspredit}
interpretation of WARMA inside the package's object-oriented
sliding-window pipeline. In other words, it is an adaptation aligned with
\code{ts_regsw}, not a separate estimation framework detached from the rest of the
library.
}
\examples{
data(tsd)

ts <- ts_data(tsd$y, 8)
samp <- ts_sample(ts, test_size = 5)
io_train <- ts_projection(samp$train)
io_test <- ts_projection(samp$test)

model <- ts_warma(input_size = 5, steps = NA)
model <- fit(model, io_train$input, io_train$output)

prediction <- predict(model, io_test$input[1, ], steps_ahead = 5)
prediction
}
\references{
\itemize{
\item Box GEP, Jenkins GM, Reinsel GC, Ljung GM (2015). Time Series Analysis:
Forecasting and Control. Wiley.
\item Hyndman RJ, Athanasopoulos G (2021). Forecasting: Principles and Practice.
Third Edition. OTexts. https://otexts.com/fpp3/
\item Ogasawara E, Pereira ACM, Bernardes GFR, Brandão AAF, Albuquerque MP
(2010). Adaptive normalization: A novel data normalization approach for
non-stationary time series. IJCNN.
\item Local WARMA manuscript used as implementation reference:
2026_04_SBBD_WARMA.pdf.
}
}
