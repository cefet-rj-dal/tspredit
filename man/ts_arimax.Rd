% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_arimax.R
\name{ts_arimax}
\alias{ts_arimax}
\title{ARIMAX}
\usage{
ts_arimax(models_x = NULL, p = NULL, d = NULL, q = NULL)
}
\arguments{
\item{models_x}{Optional named list with one univariate model per auxiliary
variable.}

\item{p}{Optional integer autoregressive order. Leave \code{NULL} to let
\code{auto.arima()} choose it.}

\item{d}{Optional integer differencing order. Leave \code{NULL} to let
\code{auto.arima()} choose it.}

\item{q}{Optional integer moving-average order. Leave \code{NULL} to let
\code{auto.arima()} choose it.}
}
\value{
A \code{ts_arimax} object inheriting from \code{ts_reg_mv}.
}
\description{
Create a target-centered multivariate regressor based on ARIMA
with external regressors.
}
\details{
\code{ts_arimax()} is the singular multivariate counterpart of \code{ts_arima()}.

The model keeps one target variable \code{y} as the main forecasting objective and
uses the aligned auxiliary variables \verb{x1, ..., xn} as regressors through the
\code{xreg} mechanism of the \code{forecast} package.

This is the natural choice when the user thinks in the following way:
\itemize{
\item there is one main series \code{y}
\item the remaining variables help explain or anticipate \code{y}
\item the primary output is the future path of \code{y}
}

In other words, \code{ts_arimax()} is a target-centered multivariate model, not a
symmetric system model like \code{ts_var()}.

For multi-step forecasting, future auxiliary values can be supplied directly
or generated by the auxiliary univariate models stored in \code{models_x}.

This makes \code{ts_arimax()} a natural member of the new \code{ts_reg_mv} branch:
\itemize{
\item the target forecast remains central
\item the aligned multivariate system is still available when \code{return_all = TRUE}
\item auxiliary series can be modeled separately when their future path is not
known beforehand
}

The current implementation follows the same philosophy as \code{ts_arima()}:
\itemize{
\item if \code{p}, \code{d}, and \code{q} are supplied, fit that specific order
\item otherwise use \code{forecast::auto.arima()} on the target with \code{xreg}
}

This keeps the singular multivariate branch consistent with the existing raw
univariate branch of the package.
}
\examples{
data(tsd)
x1 <- c(tsd$y[-1], tail(tsd$y, 1))
x2 <- stats::filter(tsd$y, rep(1/3, 3), sides = 1)
x2[is.na(x2)] <- tsd$y[is.na(x2)]

mv <- ts_data_mv(data.frame(y = tsd$y, x1 = x1, x2 = as.numeric(x2)), y = "y")
samp <- ts_sample(mv, test_size = 5)

model <- ts_arimax(models_x = list(x1 = ts_arima(), x2 = ts_arima()))
model <- fit(model, samp$train)
predict(model, steps_ahead = 5)
}
\references{
\itemize{
\item Box GEP, Jenkins GM, Reinsel GC, Ljung GM (2015). Time Series Analysis:
Forecasting and Control. Wiley.
\item Hyndman RJ, Athanasopoulos G (2021). Forecasting: Principles and Practice.
Third Edition. OTexts. https://otexts.com/fpp3/
}
}
