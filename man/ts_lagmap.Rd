% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_lagmap.R
\name{ts_lagmap}
\alias{ts_lagmap}
\title{Lag Mapping for Sliding-Window Predictors}
\usage{
ts_lagmap(
  method = c("recent", "even", "geom", "acf", "pacf", "peaks", "seasonal",
    "acf_seasonal", "pacf_seasonal", "blocks", "mi", "mrmr"),
  seasonality = NULL,
  peak_basis = c("acf", "pacf"),
  block_radius = 1,
  bins = 8
)
}
\arguments{
\item{method}{Character. Lag-selection strategy:
\code{"recent"}, \code{"even"}, \code{"geom"}, \code{"acf"}, \code{"pacf"}, \code{"peaks"},
\code{"seasonal"}, \code{"acf_seasonal"}, \code{"pacf_seasonal"}, \code{"blocks"}, \code{"mi"},
or \code{"mrmr"}.}

\item{seasonality}{Optional integer. Seasonal period used by the seasonal
lag selectors. If \code{NULL}, an estimate is derived from the training series.}

\item{peak_basis}{Character. Correlation profile used by \code{"peaks"} and
\code{"blocks"}: \code{"acf"} or \code{"pacf"}.}

\item{block_radius}{Integer. Radius around each selected center when
\code{method = "blocks"}.}

\item{bins}{Integer. Number of quantile bins used by the mutual-information
criteria.}
}
\value{
A \code{ts_lagmap} object.
}
\description{
Configure how a sliding-window predictor chooses the \code{input_size} lagged
attributes that will be fed to the underlying regression model.
}
\details{
The lag mapper is fitted on the training data before the base predictor is
trained. During \code{fit()}, the mapper stores a vector of selected lag columns.
The default \code{"recent"} method reproduces the historical behavior of the
package: it keeps the most recent \code{input_size} observations available in the
sliding window.

When \code{ts_lagmap()} is used inside a \code{ts_regsw} model, the mapper is fitted
after the model preprocessor has transformed the training windows. So the
selected positions refer to the representation actually seen by the backend,
not necessarily to the raw pre-transform window geometry.

Correlation-based methods operate on the raw training series reconstructed
from the input windows and aligned outputs. Supervised methods (\code{"mi"} and
\code{"mrmr"}) inspect the relationship between each lagged attribute and the
training target.
}
\examples{
library(daltoolbox)
library(tspredit)
data(tsd)

ts <- ts_data(tsd$y, 10)
io <- ts_projection(ts)

mapper <- ts_lagmap(method = "pacf")
mapper <- fit(mapper, io$input, io$output, input_size = 4)
mapper$lags
mapper$columns
}
\references{
\itemize{
\item Box GEP, Jenkins GM, Reinsel GC, Ljung GM (2015). Time Series Analysis:
Forecasting and Control. Fifth Edition. Wiley.
\item Peng H, Long F, Ding C (2005). Feature selection based on mutual
information criteria of max-dependency, max-relevance, and min-redundancy.
IEEE Transactions on Pattern Analysis and Machine Intelligence, 27(8),
1226-1238. doi:10.1109/TPAMI.2005.159
\item Leites J, Cerqueira V, Soares C (2024). Selecting time lags for time
series forecasting: an empirical study. arXiv:2405.11237.
}
}
