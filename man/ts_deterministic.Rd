% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_deterministic.R
\name{ts_deterministic}
\alias{ts_deterministic}
\title{Deterministic Univariate Predictor}
\usage{
ts_deterministic(
  mode = c("periodic", "persist"),
  period = NULL,
  context_size = NULL
)
}
\arguments{
\item{mode}{Character. Deterministic mode. Supported values are
\code{"periodic"} and \code{"persist"}.}

\item{period}{Optional integer. Required when \code{mode = "periodic"}.}

\item{context_size}{Optional integer. Number of most recent values used to
identify the next state in a periodic cycle. When omitted, the smallest
non-ambiguous context is inferred from the learned cycle.}
}
\value{
A \code{ts_deterministic} object.
}
\description{
Forecast a univariate series using a deterministic law of
formation instead of a statistical learner.
}
\details{
\code{ts_deterministic()} defines a small family of rule-based predictors that
can operate either on raw time series or on sliding-window inputs.

The current deterministic modes are:
\itemize{
\item \code{"periodic"}: repeat a learned cycle of fixed length
\item \code{"persist"}: repeat the most recent observed value
}

This family is useful for variables whose future behavior is structurally
determined, such as:
\itemize{
\item day-of-week codes
\item weekend indicators
\item fixed operational calendars
\item slowly changing auxiliary variables
}

Because the forecasting rule is deterministic, the same object can be used
in two contexts:
\itemize{
\item direct raw-series prediction, in the lineage of \code{ts_arima()}
\item sliding-window prediction, in the lineage of \code{ts_regsw}
}

In other words, \code{ts_deterministic()} unifies both views for cases where the
predictive mechanism is a rule, not a learner over lagged attributes.
}
\examples{
series <- c(4, 5, 6, 7, 1, 2, 3)
model <- ts_deterministic("periodic", period = 7)
model <- daltoolbox::fit(model, x = series)
predict(model, steps_ahead = 5)

sw <- ts_data(series, sw = 4)
io <- ts_projection(sw)
model <- daltoolbox::fit(ts_deterministic("persist"), io$input, io$output)
predict(model, io$input[1:2, ], steps_ahead = 1)
}
